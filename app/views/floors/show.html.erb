<p style="color: green"><%= notice %></p>

<div style="margin-bottom: 1rem;">
  <%= link_to "← Back to Floors", floors_path %>
</div>

<h1>Floor Details</h1>

<%= render @floor %>

<div style="margin-top: 2rem;">
  <%= link_to "Edit", edit_floor_path(@floor), class: "button-secondary" %>
  <%= button_to "Delete", @floor, method: :delete, class: "button-danger", form: { data: { turbo_confirm: "Are you sure you want to delete this floor and all its rooms?" } } %>
</div>

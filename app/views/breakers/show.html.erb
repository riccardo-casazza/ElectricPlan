<p style="color: green"><%= notice %></p>

<div style="margin-bottom: 1rem;">
  <%= link_to "← Back to Breakers", breakers_path %>
</div>

<h1>Breaker Details</h1>

<%= compliance_alerts_for(@breaker, include_upstream: true, include_downstream: true) %>

<%= render @breaker %>

<div style="margin-top: 2rem;">
  <%= link_to "Edit", edit_breaker_path(@breaker), class: "button-secondary" %>
  <%= button_to "Delete", @breaker, method: :delete, class: "button-secondary", form: { data: { turbo_confirm: "Are you sure you want to delete this breaker?" } } %>
</div>

<p style="color: green"><%= notice %></p>

<div style="margin-bottom: 1rem;">
  <%= link_to "← Back to RCDs", residual_current_devices_path %>
</div>

<h1>RCD Details</h1>

<%= compliance_alerts_for(@residual_current_device, include_downstream: true) %>

<%= render @residual_current_device %>

<div style="margin-top: 2rem;">
  <%= link_to "Edit", edit_residual_current_device_path(@residual_current_device), class: "button-secondary" %>
  <%= button_to "Delete", @residual_current_device, method: :delete, class: "button-danger", form: { data: { turbo_confirm: "Are you sure you want to delete this RCD?" } } %>
</div>

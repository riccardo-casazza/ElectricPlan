json.extract! residual_current_device, :id, :electrical_panel_id, :row_number, :position, :max_current, :residual_current_device_type_id, :created_at, :updated_at
json.url residual_current_device_url(residual_current_device, format: :json)

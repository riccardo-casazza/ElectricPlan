<p style="color: green"><%= notice %></p>

<div style="margin-bottom: 1rem;">
  <%= link_to "← Back to Items", items_path %>
</div>

<h1>Item Details</h1>

<%= compliance_alerts_for(@item, include_upstream: true) %>

<%= render @item %>

<div style="margin-top: 2rem;">
  <%= link_to "Edit", edit_item_path(@item), class: "button-secondary" %>
  <%= button_to "Delete", @item, method: :delete, class: "button-danger", form: { data: { turbo_confirm: "Are you sure you want to delete this item?" } } %>
</div>

<%= form_with(model: rule) do |form| %>
  <% if rule.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rule.errors.count, "error") %> prohibited this rule from being saved:</h2>

      <ul>
        <% rule.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.textarea :description %>
  </div>

  <div>
    <%= form.label :rule, style: "display: block" %>
    <%= form.textarea :rule %>
  </div>

  <div>
    <%= form.label :applies_to, style: "display: block" %>
    <%= form.text_field :applies_to %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

<p style="color: green"><%= notice %></p>

<div style="margin-bottom: 1rem;">
  <%= link_to "← Back to RCD Types", residual_current_device_types_path %>
</div>

<h1>RCD Type Details</h1>

<%= render @residual_current_device_type %>

<div style="margin-top: 2rem;">
  <%= link_to "Edit", edit_residual_current_device_type_path(@residual_current_device_type), class: "button-secondary" %>
  <%= button_to "Delete", @residual_current_device_type, method: :delete, class: "button-danger", form: { data: { turbo_confirm: "Are you sure you want to delete this RCD type?" } } %>
</div>

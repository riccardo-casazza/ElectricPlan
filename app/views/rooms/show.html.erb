<p style="color: green"><%= notice %></p>

<div style="margin-bottom: 1rem;">
  <%= link_to "← Back to Rooms", rooms_path %>
</div>

<h1>Room Details</h1>

<%= render @room %>

<div style="margin-top: 2rem;">
  <%= link_to "Edit", edit_room_path(@room), class: "button-secondary" %>
  <%= button_to "Delete", @room, method: :delete, class: "button-danger", form: { data: { turbo_confirm: "Are you sure you want to delete this room?" } } %>
</div>

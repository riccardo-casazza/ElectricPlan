<p style="color: green"><%= notice %></p>

<div style="margin-bottom: 1rem;">
  <%= link_to "← Back to Item Types", item_types_path %>
</div>

<h1>Item Type Details</h1>

<%= render @item_type %>

<div style="margin-top: 2rem;">
  <%= link_to "Edit", edit_item_type_path(@item_type), class: "button-secondary" %>
  <%= button_to "Delete", @item_type, method: :delete, class: "button-danger", form: { data: { turbo_confirm: "Are you sure you want to delete this item type?" } } %>
</div>

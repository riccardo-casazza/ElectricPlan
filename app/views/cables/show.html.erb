<p style="color: green"><%= notice %></p>

<div style="margin-bottom: 1rem;">
  <%= link_to "← Back to Cables", cables_path %>
</div>

<h1>Cable Details</h1>

<%= render @cable %>

<div style="margin-top: 2rem;">
  <%= link_to "Edit", edit_cable_path(@cable), class: "button-secondary" %>
  <%= button_to "Delete", @cable, method: :delete, class: "button-danger", form: { data: { turbo_confirm: "Are you sure you want to delete this cable?" } } %>
</div>
